<resources>

    <color name="primary">#27344D </color>
    <color name="primaryDark">#27344D </color>
    <color name="accent">#1C75BB </color>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primaryDark</item>
        <item name="android:colorAccent">@color/accent</item>

        <item name="android:windowBackground">@color/primary</item>

        <item name="android:spinnerStyle">@style/AppSpinnerTheme</item>
    </style>

    <style name="AppSpinnerTheme" parent="Base.Widget.AppCompat.Spinner">
        <item name="android:backgroundTint">#FFFFFF</item>
    </style>

</resources>
